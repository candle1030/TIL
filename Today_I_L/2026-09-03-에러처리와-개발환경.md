# 자주 만나는 에러 유형과 개발 환경 정리

**날짜:** 2026-09-03 (목)

---

## 1. 구글 코랩

파이썬 코드를 실행할 수 있는 링크(클라우드 노트북 환경).

## 2. 자주 만나는 에러

**ModuleNotFoundError (모듈을 찾을 수 없음)**
- 뜻: "내 컴퓨터에 그런 이름의 라이브러리(도구 상자)가 없다"는 상황.
- 해결법: 터미널(콘솔)에 `pip install 패키지이름`을 입력해 설치한다.

**FileNotFoundError (파일을 찾을 수 없음)**
- 뜻: 코드가 파일을 읽어오라고 했는데 지정한 경로에 파일이 없는 상황.
- 해결법: 파일 이름에 오타가 없는지, 파일이 지금 작업 중인 폴더(경로) 안에 있는지 확인한다.

**Notebook 커널 불일치 (Jupyter Notebook Kernel Mismatch)**
- 뜻: 노트북 화면을 띄운 프로그램(브라우저)과 실제로 계산하는 파이썬 커널의 짝이 맞지 않는 상황.
- 해결법: 주피터 노트북 우측 상단의 커널 메뉴에서, 패키지를 설치한 가상환경/올바른 파이썬 버전으로 커널을 바꾼다.

## 3. GitHub 저장소를 쓰는 개발 흐름

1. VS Code에 들어간다.
2. Python을 실행한다.
3. `.venv` 가상환경을 연다.
4. `requirements.txt`에 필요한 패키지를 작성한다.
5. Jupyter Notebook을 연다.
6. 샘플 데이터를 확인한다.
7. 실행 결과를 검증한다.

## 4. 환경 설정이 먼저인 이유

- 데이터 분석 파일에서는 csv 파일을 많이 사용한다.
- `sys.executable`과 `Path.cwd()`로 현재 실행 환경을 확인할 수 있다.
- 환경을 미리 정리하지 않으면 같은 코드를 다시 실행하거나 다른 PC에서 재현하기 어려워진다.
