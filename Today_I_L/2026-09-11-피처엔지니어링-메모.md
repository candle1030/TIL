# Feature/Target 구분과 결측치 처리 메모

**날짜:** 2026-09-11 (금) 오후 · AI

---

## 핵심 메모

- `.duplicated()`: 컬럼이 완전히 동일한 행을 찾는 코드.
- **Target(정답)**: 맞히고 싶은 정답.
- **Feature**: 예측에 사용할 입력 정보.
- `.fillna()`: 결측치를 다른 값으로 대체할 때 사용하는 코드.

**피처 후보 분류**

| 구분 | 의미 |
|---|---|
| `KEEP` | 그대로 사용할 후보 |
| `EXCLUDE` | 모델 입력에서는 제외할 후보 |
| `DERIVE` | 새로운 Feature를 만들 재료 |
| `DEFER` | 지금 결정하지 않고 보류 |
