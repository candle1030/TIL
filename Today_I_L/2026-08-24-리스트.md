# 리스트 슬라이싱과 수정 (append/insert/extend/remove/pop/del)

**날짜:** 2026-08-24 (월) 오전

---

## 1. 슬라이싱으로 여러 값 가져오기

```python
numbers = [10, 20, 30, 40, 50]
print(numbers[1:4])  # [20, 30, 40]
print(numbers[:3])   # [10, 20, 30]
print(numbers[2:])   # [30, 40, 50]
```

## 2. 리스트 값 수정하기

리스트의 값은 수정할 수 있다.

```python
scores = [80, 90, 70]
print(scores)        # [80, 90, 70]
scores[2] = 95
print(scores)        # [80, 90, 95]
```

## 3. 조건에 맞는 값 합산하기

```python
total_high = 0
for score in scores:
    if score > 51:
        total_high += score
```

- `total_high += score`는 `total_high = total_high + score`와 같다. 지금까지 더한 값에 계속 더해간다.

| 연산 | 계산 | 결과 |
|---|---|---|
| `total_high += 97` | 0 + 97 | 97 |
| `total_high += 77` | 97 + 77 | 174 |
| `total_high += 100` | 174 + 100 | 274 |

## 4. 값 추가하기 - append()

```python
fruits = ["사과", "바나나"]
fruits.append("포도")
print(fruits)   # ['사과', '바나나', '포도']
```

빈 리스트에서 시작해 값 세 개를 추가하는 예:

```python
todo = []
todo.append("책 읽기")
todo.append("운동하기")
todo.append("복습하기")
print(todo)   # ['책읽기', '운동하기', '복습하기']
```

## 5. 리스트 메서드 정리

| 값 추가 | 기능 | 값 삭제 | 기능 |
|---|---|---|---|
| `append` | 맨 뒤에 값 하나 추가 | `remove` | 값을 찾아 삭제 |
| `insert` | 원하는 위치에 값 하나 추가 | `pop` | 위치로 삭제하고 값 반환 |
| `extend` | 여러 값을 이어 붙임 | `del items` | 위치로 바로 삭제 |

- `insert(1, "파이썬")`: 지정한 위치(1)에 "파이썬"을 추가한다 — 원하는 위치에 원하는 값을 추가하는 기능.
- `append`: 리스트 뒤에 값을 추가하는 기능.
- `extend`: 여러 개의 값을 한꺼번에 추가하는 기능.
- `pop`: 지정한 위치의 값을 삭제하고, 삭제한 값을 반환하는 기능.
- `del items`: 지정한 값을 완전히 삭제하는 기능.
