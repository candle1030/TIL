# 데이터 분석과 pandas 시작하기 (chapter21)

**날짜:** 2026-09-01 (화) 오전

---

## 1. 환경 설정 및 라이브러리 불러오기

**csv란?** 표를 글자로 저장하는 방법. Comma-Separated Values(쉼표로 구분된 값)의 약자.

> 괄호가 있는 것은 객체의 메서드, 괄호가 없는 것은 객체의 속성이다.

- `python -m pip`: 파이썬 패키지 관리자 pip를 현재 환경에서 실행하겠다는 명령어. 외부 라이브러리(예: pandas)를 설치·업그레이드할 때 터미널에서 사용한다.
- `import pandas as pd`: 파이썬의 데이터 분석 라이브러리 pandas를 불러오는 명령어.
- `Path` (pathlib): 파일이나 폴더의 경로를 객체로 다루는 모듈. 운영체제(윈도우, macOS 등)에 상관없이 파일 경로를 안전하고 편리하게 다룰 수 있게 해준다.

## 2. 파이썬 기본 함수

- `print()`: 괄호 안 내용을 화면에 출력한다.
- `type()`: 전달된 데이터의 자료형(int, float, str, DataFrame 등)이 무엇인지 알려준다.

## 3. 데이터 구조 및 불러오기

- `pd.Series()`: 판다스의 1차원 자료구조. 엑셀의 한 열(Column) 또는 한 행(Row)처럼 값이 한 줄로 나열된 형태.
- `pd.DataFrame()`: 판다스의 2차원 자료구조. 행과 열이 있는 엑셀 시트와 같은 형태로, 데이터 분석 대부분의 작업이 이 위에서 이루어진다.
- `pd.read_csv()`: csv 파일을 읽어와 DataFrame으로 만들어주는 함수.

## 4. 데이터 구조 확인 및 탐색

| 함수/속성 | 뜻 | 설명 |
|---|---|---|
| `head()` | 처음 몇 개 행 보기 | 숫자를 넣지 않으면 기본 5개. 데이터가 잘 불러와졌는지 확인할 때 사용 |
| `tail()` | 마지막 몇 개 행 보기 | 기본값은 마지막 5개. 데이터 끝부분 확인할 때 사용 |
| `shape` | (행 개수, 열 개수) | 속성이므로 괄호를 붙이지 않는다 |
| `columns` | 모든 열 이름 목록 | 어떤 항목들이 있는지 확인할 때 사용 |
| `dtypes` | 각 열의 데이터 타입 | 숫자가 문자로 잘못 인식되지 않았는지 확인할 때 유용 |
| `info()` | 전반적인 요약 정보 | 행/열 개수, 데이터 타입, 결측치 개수를 한눈에 파악 |

## 5. 데이터 선택 및 조건 필터링

- **열 선택**: `df['열이름']`처럼 대괄호로 원하는 열만 골라낸다.
- **Boolean Series**: 조건에 맞는지 True/False로 채워 반환하는 시리즈. (예: "나이가 30 이상인가?")
- **조건 필터링**: Boolean Series를 이용해 조건에 맞는 행만 골라내는 작업.

```python
df[df['나이'] >= 30]
```

- **`&`와 괄호**: 여러 조건을 동시에 만족(AND)해야 할 때 사용하며, 각 조건은 반드시 괄호로 묶어야 오류가 나지 않는다.

```python
(df['나이'] >= 30) & (df['지역'] == '서울')
```

## 6. 통계 및 요약

- `mean()`: 평균
- `min()`: 최솟값
- `max()`: 최댓값
- `sum()`: 합계
- `describe()`: 개수, 평균, 표준편차, 최솟값, 4분위수, 최댓값 등 기초 통계량을 한 번에 요약
- `isna().sum()`: 각 열마다 결측치(NaN)가 몇 개인지 센다. `isna()`가 빈 값을 True로 표시하고, `.sum()`이 그 개수를 더한다. 데이터 정제 과정에서 필수적으로 사용.
