# 문자열 인덱싱과 슬라이싱, 문자열 메서드 (chapter07)

**날짜:** 2026-08-20 (목) 오후

---

## 1. 인덱싱과 슬라이싱

```python
word = "python"
```

**인덱싱**
```python
word[0]    # "p"
word[-1]   # "n"
```

**슬라이싱**
```python
word[0:2]  # "py"
word[2:6]  # "thon"
```

**인덱스 번호표**

```
 0    1    2    3    4    5
"p"  "y"  "t"  "h"  "o"  "n"
-6   -5   -4   -3   -2   -1
```

> 슬라이싱은 끝 번호 직전까지라는 것을 기억하면 좋다.

```python
email = "student@example.com"

print(email[0:7])   # "student"
print(email[8:])    # "example.com"
```

## 2. 자주 쓰는 문자열 메서드

```python
text = "python"
print(text.upper())
```

- `upper()`: 문자열의 영문자를 대문자로 바꾼 새 문자열을 만든다.
- `lower()`: 문자열의 영문자를 소문자로 바꾼 새 문자열을 반환한다.
- `strip()`: 양쪽 끝의 공백을 제거한다. (중요: 문자열 중간의 공백은 제거하지 않는다.)
- `replace()`: 문자열에서 지정한 부분을 다른 문자열로 바꾼 새 문자열을 반환한다.
