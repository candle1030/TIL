# 함수 정의와 매개변수, return과 print의 차이 (chapter14)

**날짜:** 2026-08-26 (수)

---

함수란 특정 작업을 하나로 묶어서 이름을 붙여 놓은 것이다.

## 1. 함수 정의와 호출

```python
def greet():
    print("안녕하세요")
    print("파이썬 공부를 시작합니다.")

greet()
greet()
```

- `def greet():` → 할 일을 이름으로 묶어 둔다.
- `greet()` → 묶어 둔 코드를 실제로 실행한다.

순서: **def로 함수 만들기 → 함수 이름() 으로 함수 실행하기**

```python
def say_hello():
    print("hello, Python!")
```

- `greet` 같은 함수 이름은 원하는 이름으로 바꿀 수 있다.

## 2. 매개변수가 있는 함수

```python
def show_profile(name, age, city):
    print(f"이름: {name}")
    print(f"나이: {age}")
    print(f"도시: {city}")

show_profile("민수", 20, "서울")
```

## 3. 매개변수 vs 인수

```python
def add(a, b):
    return a + b

add(10, 20)   # 호출할 때 전달하는 실제 값(인수)
```

```python
def greet(name):
    print(f"안녕하세요, {name}님!")

greet("민수")
```

- **매개변수**: 함수 정의에서 값을 받을 자리(나중에 받을 자리를 미리 만들어 둔 것).
- **인수**: 함수를 호출할 때 실제로 전달하는 값.

## 4. return vs print

| | 하는 일 |
|---|---|
| `print` | 결과를 화면에 보여준다 |
| `return` | 함수의 결과를 함수 밖으로 내보낸다 |

`return`은 결과를 저장해서 나중에 다시 사용할 수 있다는 점이 중요하다 — 다른 계산에 다시 사용할 수 있기 때문이다.
