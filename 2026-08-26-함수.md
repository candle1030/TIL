# 함수 정의와 매개변수, return과 print의 차이 (chapter14)

_2026-08-26 (수) 오전_

---

8월 26일 예비군 불참 chapter14

*함수란 특정 작업을 하나로 묶어서 이름을 붙여 놓은 것.*



*
def greet():
	print("안녕하세요")
	print("파이썬 공부를 시작합니다.")
greet()
greet()
		def greet():
			print("안녕하세요")


함수 호출
		greet()
본문 실행
		안녕하세요

def greet():		할 일을 이름으로 묶어 둠
greet()		묶어 둔 코드를 실제로 실행

def 함수 이름():
	실행할 코드

def say_hello():
	print("hello, Python!")
def greet(): 에서 greet(): 는 변수의 이름으로 바꿀 수 있다.

				순서
	def -> 함수 만들기 -> 함수 이름() -> 함수 실행하기.

**
def show_profile(name, age, city):
	print(f"이름: {name}")
	print(f"나이: {age}")
    	print(f"도시: {city}")
show_profile("민수", 20, "서울")


***
add(10, 20)
호출할 때 전달하는 실제 값

def add(a, b):
	return a + b


****
매개 변수

def greet(name):
	print(f"안녕하세요, {name}님!")

greet("민수")
	여기서 name는 매개변수		나중에 받을 자리를 미리 만들어 놓은 것.
	매개변수는 함수 정의에서 값을 받을 자리, 인수는 호출할 떄 실제로 전달하는 값.



*****
print와 같이 return도 출력을 하지만 return은 결과를 저장하고 나중에 다시 사용할 수 있음.

print는 출력 값을 화면에 보여준다

return은 함수의 결과를 함수 밖으로 보내준다


								**리턴과 프린트의 차이**
						둘의 차이점은	print는 보여주는 것
									return은 밖으로 보내는 것
									의 차이.

return이 중요한 이유는 다른 계산에 다시 사용할 수 있기 때문에 중요함.
