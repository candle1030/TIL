# 반복문 for·while과 range() (chapter10)

**날짜:** 2026-08-21 (금) 오후

---

## 1. for문 기본

for문은 값을 하나씩 꺼내 같은 코드를 반복한다.

```python
for i in range(5):
    print(i)
```
```
0 1 2 3 4
```

동작 순서:
1. `range()`가 반복할 값을 준비한다.
2. `for`가 값을 하나씩 반복 변수에 넣는다.
3. 들여쓴 코드를 실행한다.

## 2. range()의 세 가지 형태

```python
range(5)         # 0 1 2 3 4         (끝만 지정)
range(1, 6)      # 1 2 3 4 5         (시작, 끝 지정)
range(2, 11, 2)  # 2 4 6 8 10        (시작, 끝, 간격 지정 - 짝수만)
range(5, 0, -1)  # 5 4 3 2 1         (역순도 가능)
```

## 3. 합계 구하기

```python
total = 0
for number in range(1, 6):
    total += number
print(total)   # 15
```

| 단계 | 계산 | total |
|---|---|---|
| 시작 | - | 0 |
| +1 | 0+1 | 1 |
| +2 | 1+2 | 3 |
| +3 | 3+3 | 6 |
| +4 | 6+4 | 10 |
| +5 | 10+5 | 15 |

## 4. 조건과 함께 쓰기

```python
for number in range(1, 6):
    if number % 2 == 0:
        print(f"{number}는 짝수입니다.")
```
```
2는 짝수입니다.
4는 짝수입니다.
```

## 5. while 반복문

while은 조건이 True인 동안만 반복한다.

```python
count = 1
while count <= 5:
    print(count)
    count += 1
```
```
1
2
3
4
5
```

> 비밀번호 시도 횟수를 제한하는 등, 조건을 넘으면 더 이상 입력받지 못하게 할 때 사용한다.

## 6. range / for / break / continue 요약

- **range**: 처음 지정한 범위 안의 값을 만든다.
- **for**: 입력한 값을 하나씩 꺼내 반복한다.
- **break**: 조건과 같아지면 멈추고 그 전까지만 출력한다.
- **continue**: 조건과 같은 값은 건너뛰고 나머지를 출력한다.
