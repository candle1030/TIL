# 반복문 for·while과 range() (chapter10)

_2026-08-21 (금) 오후_

---

8월 21일 오후chapter10



*
for문은 값을 하나씩 꺼내 같은 코드를 반복합니다.

range(5)
i = 1 -> 1 -> 2 -> 3 -> 4
print(i)

for i in range(5):
	print(i)
출력 결과 : 0 1 2 3 4

1.	range()가 반복할 값을 준비합니다.

2.	for가 값을 하나씩 반복 변수에 넣습니다.

3.	들여쓴 코드 실행.


**
1번째		0		0
2번째 	1		1
3번째 	2		2
4번째 	3		3


***
range()는 시작 끝 간격으로 반복 범위를 만듭니다.

range(5)
0 1 2 3 4
끝만 지정

range(1, 6)
1 2 3 4 5
시작, 끝 지정

range(2, 11, 2)	짝수만 출력.
2 4 6 8 10
시작, 끝, 간격을 지정

역순도 가능.
range(5, 0, -1)
5, 4, 3, 2, 1


****
for i in range(1, 11, 1)
print(i)


*****
total = 0
for number in range(1, 6):
    total += number
print(total)

실행 결과 : 15

시작 total = 0
+1 -> total = 1
+2 -> total = 3
+3 -> total = 6
+4 -> total = 10
+5 -> total = 15


******
for number in range(1, 6):
    if number % 2 == 0:
        print(f"{number}는 짝수입니다.")

2는 짝수입니다.
4는 짝수입니다.


*******
while은 조건이 true인 동안만 반복.

count = 1
while count <= 5:
	print(count)
	count += 1

실행 결과 :
1
2
3
4
5

비밀번호 횟수가 넘었을 경우 입력하지 못하도록 할 때 사용.

*range*
처음 입력한 값의 안에 있는 값을 출력.

*for*
입력한 값을 하나씩 꺼내 출력.

*break*
처음 입력한 값과 같아지면 멈추고 그 전까지만 출력 된다.

*continue*
처음 입력한 값과 같은 값을 건너뛰고 출력.
