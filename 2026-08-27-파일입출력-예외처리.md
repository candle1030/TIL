# 파일 입출력(with open)과 예외 처리(try-except) (chapter16)

_2026-08-27 (목) 오전_

---

8월 27일 오전 chapter16

*
with open

with open("memo.txt", "r", encoding="utf-8") as file:	파일 이름 		모드		문자 인코딩
	content = file.read()					블록이 끝나면 자동으로 닫힘.


**
readline() 	한 줄만 읽으려면 사용할 수 있으ㅁ.

with open("memo.txt", "r", encoding="utf-8") as file:
    for line in file:
        print(line.strip())
**for문 사용해서 반복을 시켜 사용할 수도 있음.

*중요함 = AI한테 물어볼 것.*

with open("memo.txt", "r", encoding="utf-8") as file:
    seq = 1
    for line in file:
        print(f"{seq}번째 라인 : {line.strip()}")
        seq += 1


***
\n
new line 새로운 줄이 생김.


****
W
write를 의미 


*****
a
append를 의미


******
path로 프로젝트 기준 경로 만들기
			예시
from pathlib import Path
BASE_DIR = Path(__file__).resolve().parents[2]
file_path = BASE_DIR / "data" / "chapter16" / "memo.txt"


*******

try-except로 예상 가능한 예외 처리하기

try:
    age = int(input("나이를 입력하세요: "))
    print(f"입력한 나이: {age}")
except ValueError:
    print("나이는 숫자로 입력해 주세요.")

try에는 문제가 발생할 가능성이 있는 코드를 두고, except에는 그 문제에 대한 대응을 둠.


********

try:
    number = int(input("숫자를 입력하세요: "))
    result = 100 / number
    print(result)
except ValueError:
    print("정수를 입력해 주세요.")
except ZeroDivisionError:
    print("0으로 나눌 수 없습니다.")


********

try
→ 예외가 발생할 수 있는 작업 시도
except
→ 지정한 예외가 발생했을 때 대응
else
→ try에서 예외가 없었을 때 실행
finally
→ 성공·실패와 관계없이 마지막에 실행
