# 파일 입출력(with open)과 예외 처리(try-except) (chapter16)

**날짜:** 2026-08-27 (목) 오전

---

## 1. 파일 열기 - with open

```python
with open("memo.txt", "r", encoding="utf-8") as file:
    content = file.read()
```

- `"memo.txt"`는 파일 이름, `"r"`은 모드, `encoding="utf-8"`은 문자 인코딩이다.
- `with` 블록이 끝나면 파일이 자동으로 닫힌다.

## 2. 한 줄씩 읽기

```python
with open("memo.txt", "r", encoding="utf-8") as file:
    for line in file:
        print(line.strip())
```

`for`문으로 반복시켜 한 줄씩 읽을 수도 있다.

번호를 붙여 읽기:

```python
with open("memo.txt", "r", encoding="utf-8") as file:
    seq = 1
    for line in file:
        print(f"{seq}번째 라인 : {line.strip()}")
        seq += 1
```

## 3. 자주 쓰는 파일 모드

- `\n`: 새로운 줄(new line)이 생긴다.
- `w`: write(쓰기)를 의미한다.
- `a`: append(추가)를 의미한다.

## 4. 경로를 안전하게 다루기 (pathlib)

```python
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parents[2]
file_path = BASE_DIR / "data" / "chapter16" / "memo.txt"
```

## 5. 예외 처리 - try / except

```python
try:
    age = int(input("나이를 입력하세요: "))
    print(f"입력한 나이: {age}")
except ValueError:
    print("나이는 숫자로 입력해 주세요.")
```

`try`에는 문제가 발생할 가능성이 있는 코드를 두고, `except`에는 그 문제에 대한 대응을 둔다.

여러 예외를 각각 처리하기:

```python
try:
    number = int(input("숫자를 입력하세요: "))
    result = 100 / number
    print(result)
except ValueError:
    print("정수를 입력해 주세요.")
except ZeroDivisionError:
    print("0으로 나눌 수 없습니다.")
```

## 6. try / except / else / finally 구조

| 키워드 | 역할 |
|---|---|
| `try` | 예외가 발생할 수 있는 작업을 시도 |
| `except` | 지정한 예외가 발생했을 때 대응 |
| `else` | try에서 예외가 없었을 때 실행 |
| `finally` | 성공·실패와 관계없이 마지막에 실행 |
