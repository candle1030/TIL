# 튜플과 집합

_2026-08-24 (월) 오후_

---

8월 24일 오후


< 튜플과 집합 >


turle, set, pecking/unpecking, 합집합, 교집합, 차집합

튜플은 변경할 수 없다.

(튜플)
순서가 있고, 중복 가능. 직접 수정은 불가하다.

(집합)
순서 없음, 중복 제거, 집합 연산
1,2,2,3 	-> 	1,2,3
A|B		두 집합의 모든 고유한 값.
A&B		두 집합에 공통으로 있는 값.
A-B		A에만 있고 B에는 없는 값.


*
리스트가 허용하는 조건.
	값의 순서가 중요한가?
	만든 뒤 값을 수정할 것인가?
	같은 값의 중복을 허용할 것인가?


리스트[ ]	튜플( )튜플은 하나만 있더라도 뒤에 ,가 들어가야 함.


튜플은 직접 수정이 불가함.


**

아래의 코드는 같아 보이지만 걀과가 다름.

one_value = (10,)
not_tuple = (10)
print(type(one_value))
print(type(not_tuple))


***
튜플도 인덱싱과 슬라이싱이 가능.


****
튜플을 직접 수정을 하면 코드 오류가 생김.


*****
			리스트		튜플			집합
순서가 중요한가?	o			o			순서에 의존하지 않음.
