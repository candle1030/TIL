# 딕셔너리로 키와 값 관리하기 (chapter13)

_2026-08-25 (화) 오후_

---

8월 25일 오후 chapter13

*
딕셔너리로 키와 값 관리하기.
student["name"]
student["score"] = 95
student.get("city")

{} = 딕셔너리


student = {
	"name":"민수",
	"age":20,
	"score":85
}

print(student["name"])
student["score"] = 95
student["city"] = "서울"
	대괄호[]
딕셔너리 안에 넣을 때는 값을 입력할 변수 이름을 적고
[]안에 입력할 정보를 입력하면 값이 출력이 된다.

입력하려는 딕셔너리 안에 정보가 없으면 keyError가 발생한다.

	get()
student.get("city")

	in
변수 안에 키 존재 여부를 true/False로 확인하는 키
"score" in student
if  "score" in student:
	print(student["score"])
