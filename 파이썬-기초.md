# 파이썬 기초 개념 정리 (함수/변수/리스트/튜플/딕셔너리)

_날짜 미상 (가장 기초적인 개념 정리 노트)_

---

## 함수 - 서브 프로그램

- 함수의 이름은 중요하다.
- 함수는 `return`을 포함하며, 그래서 무조건 출력을 한다. (예: `greet`은 함수의 이름)

## 변수 - 메모리 공간의 이름

```python
x = 10
```

메모리 상의 공간에 붙인 이름.

## 리스트 - 연속된 메모리 공간 []

```python
x = [1, 2, 3]
```

`x`라는 변수는 1, 2, 3을 담고 있는 리스트다. 1, 2, 3은 원소라고 부른다. 리스트는 0번부터 숫자를 센다.

## 튜플 - 순서쌍을 확장 ()

변경할 이유가 없을 때 사용한다.

## 딕셔너리 - 바로 조회 가능

key 값으로 조회한다. 단어로 조회가 가능해서, 조회할 때 직관적이면서 빠르다.

## 이름 짓기와 추상화

변수와 함수의 이름을 짓는 것은 중요하다.

- 인덱스는 데이터가 들어있는 위치다.
- 모듈은 함수를 모아놓은 것이다.
