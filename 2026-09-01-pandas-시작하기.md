# 데이터 분석과 pandas 시작하기 (chapter21)

_2026-09-01 (화) 오전_

---

9월 1일 오전 chapter21

데이터 분석과 pandas 시작하기

			**csv는 무엇인가?**
표를 글자로 저장하는 방법
Comma-Separated Values의 약자.
쉼표로 구분된 값

**괄호가 있는 것은 객체의 메서드**

**괄호가 없는 것은 객체의 속성**


	1. 환경 설정 및 라이브러리 불러오기.
*python -m pip
뜻 : 파이썬 패키지 관리자인 pip를 현재 환경에서 실행하겠다는 명령어.
설명 : 외부 라이브러리(예pandas)를 설치하거나 업그레이드할 떄 터미널(명령 프롬프트)에서 주로 사용함.

*import pandas as pd
뜻 : 파이썬의 데이터 분석 라이브러리인 pandas를 불러오는 명령어.
설명 : 운영체제(윈도우, ios)에 상관없이 파일 경로를 안전하고 편리하게 저장할 수 있게 도와줌.

*path
뜻 : 파일이나 폴더의 경로를 객체로 다루기 위한 모듈(pathlib의 path).
설명 : 운영체제에 상관없이 파일 경로를 안전하고 편리하게 지정할 수 있게 도와줌.


	2. 파이썬 기본 함수
*print()
뜻 : 괄호 안에 있는 내용을 화면에 출력(보여주기) 하는 함수.

*type()
뜻 : 전달된 데이터의 자료형(데이터 타입)이 무엇인지 알려주는 함수.
설명 : 이 데이터가 숫자(int, float), 문자열(str), 혹은 판다스 데이터프레임(Dataframe)중 어떤 것인지 확인할 때 유용.


	3. 데이터 구조 및 불러오기.
*pd.Series()
뜻 : 판다스에서 1차원 데이터를 다루는 기본 자료구조.
설명 : 엑셀의 '한 열(Column)' 또는 '한 행(Row)'처럼 값들이 한 줄로 쭉 나열된 형태

*pd.DataFrame()
뜻 : 판다스에서 2차원 데이터를 다루는 핵심 자료구조.
설명 : 행과 열이 있는 엑셀 시트나 표와 같은 형태, 데이터 분석의 대부분 작업이 이 데이터프레임을 대상으로 이루어짐.

*pd.read_csv()
뜻 : csv파일(쉼표와 같이 구분된 텍스트 파일)을 읽어와서 데이터프레임으로 만들어주는 함수입니다.


	4. 데이터 구조 확인 및 탐색
*head()
뜻 : 데이터프레임의 처음 몇 개의 행을 보여주는 함수입니다.
설명 : 괄호 안에 숫자를 넣지 않으면 기본적으로 처음 5개 행을 보여줌. 데이터가 잘 불러와졌는지 확인할 때 사용.

*tail()
뜻 : 데이터프레임의 마지막 몇 개의 행을 보여주는 함수.
설명 : 데이터의 끝부분이 잘 들어왔는지 확인할 떄 상요하며, 기본값은 마지막 5개 행.

*shape
뜻 데이터프레임의 크기를 (행 개수, 열 개수)형태로 알려주는 속성(Attribute)
설명 : 뒤에 괄호 (())를 붙이지 않는 것에 주의. 데이터가 총 몇 행 몇 열로 이루어져 있는지 파악할 떄 사용.

*columns
뜻 : 데이터프레임의 모든 열 이름(컬럼명)들을 목록으로 보여주는 속성.
설명 : 데이터에 어떤 항목(이름, 나이, 성별)들이 들어 있는지 확인할 때 사용.

*dtypes
뜻 : 데이터프레임 안의 각 열이 어떤 데이터 타입(정수형, 실수형, 문자열 등)으로 되어 있는지 보여주는 속성.
설명 : 숫자로 계산해야 할 데이터가 문자로 잘못 인식되어 있지는 않은지 확인할 때 유용함.

*info()
뜻 : 데이터프레임의 전반적인 정보를 요약해서 보여주는 함수.
설명 : 전체 행/열의 개수, 각 열의 데이터 타입, 그리고 결측치(비어 있는 값)가 몇 개나 있는지 한눈에 파악할 수 있어 매우 자주 쓰임.


	5. 데이터 선택 및 조건 필터링
*열 선택
뜻 : 데이터프레임에서 내가 원하는 특정 열(컬럼)만 골라내는 작업.
설명 : 대괄호를 이용해 df['열이름']형태로 작성.

*Boolean Series
뜻 : 조건에 맞는지 여부를 True 또는 False 값으로 채워 반환하는 시리즈.
설명 : 예를 들어 "나이가 30 이상인가?"라는 조건을 걸면, 각 행마다 맞으면 True, 틀리면 False가 담긴 결과 리스트가 만들어짐

*조건 필터링
뜻 : Boolean Series를 이용해 전체 데이터 중 특정 조건에 맞는 행만 골라내는 작업.
설명 : df[df['나이'] >=30]과 같이 쓰면 30세 이상인 사람들의 데이터만 남김.

*& 와 괄호
뜻 : 여러 개의 조건을 동시에 만족(AND)해야 할 때 사용하는 기호(&)와 연산 순서를 정해주는 괄호().
설명 : 판다스에서는 조건 필터링 시 각 조건을 반드시 괄호로 묶어야 오류가 나지 않음.
예 : (df['나이'] >= 30) & (df['지역'] == '서울')


	6. 통계 및 요약
*mean()
뜻 : 데이터의 평균을 구하는 함수

*min()
뜻 : 데이터의 최솟값을 구하는 함수.

*max()
뜻 : 데이터의 최댓값을 구하는 함수.

*sum()
뜻 : 데이터의 종합을 구하는 함수

*describe()
뜻 : 숫자형 데이터들의 개수, 평균, 표준편차, 최솟값, 4분위수, 최댓값 등 기초 통계량을 한 번에 요약해주는 함수.
설명 : 데이터의 전체적인 분포를 빠르게 파악하고 싶을 때 유용함.

*isna() . sum()
뜻 : 각 열마다 결측치(데이터가 없는 빈 값, NaN)가 총 몇 개씩 있는지 세어주는 코드.
설명 : isna()는 비어 있는 곳을 True로 만들고, 뒤에 붙은 .sum()이 그 True(즉, 빈 값의 개수)를 모두 더해 줌. 데이터 정제 과정에서 필수적으,로 사용함.
