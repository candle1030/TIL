# 리스트 슬라이싱과 수정 (append/insert/extend/remove/pop/del)

_2026-08-24 (월) 오전_

---

8월 24일 오전

	<	 리스트	>
*
슬라이싱으로 여러 값 가져오기
numbers = [10, 20, 30, 40, 50]
print(numbers[1:4])
print(numbers[:3])
print(numbers[2:])

실행결과
[20, 30, 40]
[10, 20, 30]
[30, 40, 50]


**
리스트의 값은 수정할 수 있다.

scores = [80, 90, 70]
print(scores)
scores[2] = 95
print(scores)

실행결과
[80, 90, 70]
[80, 90, 95]
scores = [80, 90, 70]

print(scores)

scores[2] = 95

print(scores)


***

=예시=
total_high = 0

for score in scores:
    if score > 51:
        total_high += score

score += total
score = score + total는 지금까지 더한 점수에 계속 더한다.

total_high = 0
total_high += 97	0 + 97
total_high += 77	99 + 77
total_high += 100	174 + 100


****
scores = [80, 90, 70]
print(scores)
scores[2] = 95
print(scores)

실행결과

[80, 90, 70]
[80, 90, 95]


*****
값 하나 추가하기	-(append)

fruits = ["사과", "바나나"]
fruits.append("포도")
print(fruits)

실행결과

['사과', '바나나', '포도']


빈 리스트에서 시작해 값을 세 개 추가합니다.

todo = []
todo append("책 읽기")
todo append("운동하기")
todo append("복습하기")
print(todo)

실행결과

[책읽기, 운동하기, 복습하기]


******

		값 추가							값 삭제
append - 맨 뒤에 값 하나		+	|	remove - 값을 찾아 삭제			-
insert - 원하는 위치에 값 하나 추가	+	|	pop - 위치로 삭제하고 값 변환		-
extend - 여러값을 이어 붙임		+	|	del items - 위치로 바로 삭제		-






insert		를 사용할 때에는 insert(1, "파이썬") - 내가 지정한 위치 1에 파임썬을 추가하겠		다.
		내가 원하는 위치에 내가 원하는 값을 추가하는 기능


append	는 뒤에 내가 설정한 값을 추가하는 기능



extend	는 여러 개의 값을 한꺼번에 추가하는 기능


pop			 는 지정한 값을 삭제하고 삭제한 값을 변수로 반환하는 기능


del items	는 지정한 값을 완전히 삭제하는 기능
