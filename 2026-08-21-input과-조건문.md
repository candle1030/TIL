# input()과 조건문 if/elif/else (chapter08, 09)

**날짜:** 2026-08-21 (금) 오전

---

## 1. input()으로 값 입력받기

```python
name = input("이름을 입력하세요: ")
print(f"안녕하세요. {name}님!")
```
```
이름을 입력하세요 : 장충만
안녕하세요. 장충만님!
```

두 값을 입력받아 계산하는 예시:

```python
number1 = input("첫 번째 숫자를 입력하세요: ")
number2 = input("두 번째 숫자를 입력하세요: ")
result = int(number1) + int(number2)
print(f"{number1} 더하기 {number2}는(은) {result}입니다.")
```

- 괄호 안의 문자열은 사용자에게 무엇을 입력해야 하는지 안내하는 문구다.
- 입력은 `input()`, 형변환은 `int()`/`float()`, 출력은 `print(f"...")`.

## 2. 조건문 if / elif / else

```python
score = 80
if score >= 60:
    print("통과입니다.")
```

- `if`는 조건이 `True`일 때만 실행되고, `False`면 해당 블록은 건너뛴다.

```python
score = 59
print(score >= 60)   # False
```

## 3. AI 프롬프트 연습

> 나는 파이썬 조건문을 처음 배우고 있어.
> 아래 조건식을 정답 코드 없이 초보자가 이해할 수 있는 한국어 질문으로 바꿔 줘.
> - `score >= 80`
> - `age < 20`
> - `name == "민수"`
> - `price != 0`

## 4. 비교 연산자 / 논리 연산자 정리

**비교 연산자:** `>=`, `<=`, `==`, `!=`

**논리 연산자**
- `and`: 모두 참이면 True
- `or`: 하나라도 참이면 True
- `not`: 참/거짓을 반대로
