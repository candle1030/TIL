# 데이터베이스 개념(PostgreSQL/DBeaver)과 EDA 절차 (chapter03)

_2026-09-04 (금) 오후_

---

9월 4일 오후 데이터베이스 chapter03

데이터베이스, DBMS, 클라이언트와 스키마의 의미 이해하기.

postgreSQL과 DBeaver의 역할을 구분하기.

PostgreSQL :
데이터를 저장하고 SQL을 실행하는 관계형 DBMS

DBeaver :
PostgreSQL에 연결해 SQL을 작성하고 결과를 보여 주는 클라이언트

SQL 파일 :
실행 과정과 확인 기준을 다시 실행할 수 있게 보존

Git 저장소 :
SQL과 문서의 변경 이력을 선택적으로 관리

AI 도구 :
오류 설명과 SQL 초안 작성을 보조

	***************************************************

SELECT 1 + 1; 결과가2, 결과를 주는 것은 DBMS에서 줌(데이터베이스엔진)

사용자
→ DBeaver에서 SQL 작성
→ PostgreSQL에 SQL 전달
→ PostgreSQL이 SQL 실행
→ DBeaver가 결과 또는 오류 표시

^^^^^^^^^^^^^^^^^^^^^^실행되는 순서  중요^^^^^^^^^^^^^^^^파일과 경로

→ 데이터 로드

→ 크기와 표본

→ 컬럼과 타입

→ 결측치와 중복

→ 값의 범위와 범주

→ 파일 간 관계

→ 구조 요약과 기록^^^

dbeaver에 데이터베이스를 ai_database_book으로 지정.



===================================================================
오후 chapter03 데이터의 첫인상 읽기


df : dataframe

판다스에서 df로 데이터 로드한다.

데이터 프레임에 is na메서드를 사용해 결측치 확인 가능

파일 간 관계 = erd




파일(csv)과 경로
======pandas : 데이터 분석 관련된 것을 사용.	(import pandas as.pd)



→ 데이터 로드
	df = pd.read.csvfile_path)
→ 크기와 표본
df.shape
df.head()
df tall

→ 컬럼과 타입
df columns
df.dtypes
df.info()

→ 결측치와 중복
isna() sum()
→ 값의 범위와 범주

→ 파일 간 관계

→ 구조 요약과 기록




다 대 다라는 의미는 주문자가 주문을 여러개 할 수 있는 것.
데이터베이스와 데이터분석에 사용되는 방식
ERD의 부모자식관계





파일 존재
→ DataFrame 로드
판다스 로드
→ shape
df.
→ head / tail
df.
→ columns
df.
→ info / dtypes
df.
→ 결측치

→ 전체 중복

→ 주요 ID

→ 숫자·범주·날짜

→ 파일 간 관계




	**중요**
.value_counts
범주된 데이터를 보여주는 코드.
빈도수 기준 내림차순 정렬 : 기본적으로 가장 많이 등장한 데이터가 맨 위에 오도록 큰 순서대로 정렬해서 보여줌.
결측치 제외 : 기본적으로 데이터에 빈 값(NaN)이 있으면 그 개수는 세지 않고 제외함.

자주 사용하는 옵션(파라미터)
normalize=True : 개수 대신 전체 데이터 중에서 차지하는 비율(소수점)로 출력해 줌.
dropna=False : NaN(결측치)이 몇 개 있는지도 함꼐 세어 줌.
ascending=True : 기본값인 내림차순 대신 오름차순으로 정렬함.
