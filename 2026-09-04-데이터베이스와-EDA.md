# 데이터베이스 개념(PostgreSQL/DBeaver)과 EDA 절차 정리 (chapter03)

**날짜:** 2026-09-04 (금) 오후

---

## 1. 데이터베이스, DBMS, 클라이언트, 스키마

- **PostgreSQL**: 데이터를 저장하고 SQL을 실행하는 관계형 DBMS.
- **DBeaver**: PostgreSQL에 연결해 SQL을 작성하고 결과를 보여주는 클라이언트.
- **SQL 파일**: 실행 과정과 확인 기준을 다시 실행할 수 있게 보존한 것.
- **Git 저장소**: SQL과 문서의 변경 이력을 선택적으로 관리.
- **AI 도구**: 오류 설명과 SQL 초안 작성을 보조.

```sql
SELECT 1 + 1;   -- 결과: 2, 이 계산을 해서 주는 것은 DBMS(데이터베이스 엔진)
```

**실행 흐름**
1. 사용자가 DBeaver에서 SQL을 작성
2. DBeaver가 PostgreSQL에 SQL을 전달
3. PostgreSQL이 SQL을 실행
4. DBeaver가 결과 또는 오류를 표시

DBeaver에서 사용한 데이터베이스 이름: `ai_database_book`

## 2. 데이터의 첫인상 읽기 (EDA 순서)

데이터를 처음 마주했을 때 살펴보는 순서:

1. 데이터 로드
2. 크기와 표본 확인
3. 컬럼과 타입 확인
4. 결측치와 중복 확인
5. 값의 범위와 범주 확인
6. 파일 간 관계 확인
7. 구조 요약과 기록

```python
import pandas as pd

df = pd.read_csv(file_path)   # 데이터 로드

df.shape        # 크기 (행, 열)
df.head()       # 상위 표본
df.tail()       # 하위 표본

df.columns      # 컬럼 목록
df.dtypes       # 컬럼별 타입
df.info()       # 컬럼과 타입 요약

df.isna().sum() # 결측치 확인
```

- `df`는 dataframe을 의미하며, pandas에서 `df`라는 이름으로 데이터를 로드하는 관례가 있다.
- `isna()`로 데이터프레임에 결측치가 있는지 확인할 수 있다.
- 파일 간의 관계는 ERD(Entity Relationship Diagram)로 표현한다.
- "다 대 다"는 예를 들어 한 주문자가 주문을 여러 개 할 수 있는 관계를 의미하며, 데이터베이스와 데이터 분석에서 ERD의 부모-자식 관계를 표현할 때 사용된다.

## 3. value_counts() 자세히

`value_counts()`는 범주형 데이터의 빈도를 보여주는 코드다.

- **기본 정렬**: 빈도수 기준 내림차순 — 가장 많이 등장한 데이터가 맨 위에 오도록 정렬된다.
- **결측치 제외**: 기본적으로 빈 값(NaN)은 개수에 포함하지 않는다.

자주 쓰는 옵션(파라미터):

| 옵션 | 의미 |
|---|---|
| `normalize=True` | 개수 대신 전체에서 차지하는 비율(소수점)로 출력 |
| `dropna=False` | NaN(결측치)의 개수도 함께 센다 |
| `ascending=True` | 기본값인 내림차순 대신 오름차순으로 정렬 |
