# 변수, 자료형, 비교·논리 연산자 (chapter05, 06)

**날짜:** 2026-08-20 (목) 오전

---

## 1. 형 변환과 변수

- `int("홍길동")` → `ValueError` 발생: 숫자가 아닌 문자열을 숫자로 바꾸려 하면 오류가 난다.
- `str(20)` → 가능: 숫자는 문자열로 변환할 수 있다.

```python
member_name = "이순신"
print(member_name)
```

## 2. 변수는 위에서 아래로 읽힌다

같은 이름의 변수가 아래에서 다시 선언되기 전까지는, 코드는 위에서 아래로 순서대로 실행되며 그 시점까지 저장된 값을 출력한다.

```python
name = "문길동"
age = 20
city = "서울"
nema = "홍길동"  # 오타: name이 아니라 nema

print(name)   # 문길동
print(age)    # 20
print(city)   # 서울
```

> 오타(`nema`)가 나면 원래 의도한 `name`과는 별개의 변수가 되어버리므로 출력 결과가 헷갈릴 수 있다.

## 3. 예약어는 변수 이름으로 쓸 수 없다

`if`, `for`, `class`같은 예약어(keyword)는 변수 이름으로 사용할 수 없다.

## 4. 불리언(bool)

- `1` → 참(True)으로도 사용 가능
- `0` → 거짓(False)으로도 사용 가능

## 5. type() 확인

```python
print(type(age))
```

## 6. 비교 연산자

| 식 | 결과 | 의미 |
|---|---|---|
| `10 == 10` | True | 10은 10과 같은가? |
| `10 != 3` | True | 10은 3과 다른가? |
| `10 > 3` | True | 10은 3보다 큰가? |
| `10 < 3` | False | 10은 3보다 작은가? |
| `10 >= 10` | True | 10은 10보다 크거나 같은가? |
| `3 <= 10` | True | 3은 10보다 작거나 같은가? |

## 7. 논리 연산자

| 식 | 결과 |
|---|---|
| `True and True` | True |
| `True and False` | False |
| `True or True` | True |
| `False or False` | False |
| `not True` | False |

## 8. 연산 순서와 괄호

연산 순서가 헷갈리면 괄호를 사용한다.

```python
2 + 3 * 4      # 14
(2 + 3) * 4    # 20
```
